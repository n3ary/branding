# pnpm 11 refuses to expand env vars in project .npmrc (the file
# is committed and could leak the secret to an attacker-controlled
# registry). This project has no private registry auth needs; the
# file exists only so a future consumer scope (e.g. @n3ary/*) has
# a consistent baseline.
